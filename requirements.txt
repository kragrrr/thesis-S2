# GPU (e.g. RTX 4090 on Windows): install CUDA PyTorch *first*, then the rest — otherwise pip may pull +cpu:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
ultralytics>=8.3.0
torch>=2.1.0
torchvision>=0.16.0
numpy
Pillow
opencv-python-headless
matplotlib
seaborn
scikit-learn
pyyaml
requests
tqdm
pandas
